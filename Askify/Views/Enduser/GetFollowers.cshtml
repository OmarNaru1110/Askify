@model List<EndUser>
@{
    ViewData["Title"] = "Followers";
}
<div class="list-group">
    @foreach (var user in Model)
    {
        <a class="list-group-item" asp-action="toanotherprofile" asp-route-endUserId="@user.Id">
            <h3>@user.UserName</h3>
        </a>
    }
</div>
